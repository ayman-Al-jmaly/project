🔵 strlen() في PHP
دالة strlen() هي إحدى أهم دوال النصوص في PHP، وتستخدم لحساب عدد الحروف (characters) الموجودة داخل سلسلة نصية.

✔ ترجع عدد الحروف داخل السلسلة
✔مع المسافات 
✔ والرموز 
✔ والأرقام 
دالة strlen() تُرجِع عدد البايتات وليس عدد الحروف في حالة الأحرف العربية.
مثال:
echo strlen("مرحبا");
قد يعطي:  10
لأن الحرف العربي يُخزّن بـ 2 بايت وربما أكثر.
الحل لحساب عدد الحروف العربية:
استخدم:
mb_strlen("مرحبا", "UTF-8");
النتيجة: 5
🎯 استخدامات شائعة لـ strlen()
✔ التحقق من طول كلمة المرور
✔ تحديد مدخلات المستخدم
✔ قراءة عدد الحروف قبل التخزين
✔ تصميم أنظمة تحقق Validation







🔵 str_word_count()
تُعد دالة str_word_count() واحدة من دوال النصوص المهمة في لغة PHP، وتستخدم لتحليل النصوص واستخراج عدد الكلمات أو تفاصيلها.
هي مفيدة في معالجة المحتوى، وإحصاء الكلمات، والتحقق من طول المدخلات، وإنشاء برامج الإبلاغ النصي.

تقوم str_word_count() بـ: ✔ عدّ الكلمات في النص
✔ أو استخراج الكلمات نفسها
✔ أو إرجاع الكلمات مع مواقعها في النص
وذلك حسب قيمة المتغير $format.
 مثال 
➤ 1. عدّ عدد الكلمات
$text = "Hello world from PHP";
echo str_word_count($text);
النتيجة:4
 أمثلة على جميع صيغ format
➤ 2. إرجاع الكلمات فقط (format = 1)
$text = "Hello world from PHP";
print_r(str_word_count($text, 1));
النتيجة:
Array
(
    [0] => Hello
    [1] => world
    [2] => from
    [3] => PHP
)


➤ 3. إرجاع الكلمات مع مواقعها (format = 2)
$text = "Hello world from PHP";
print_r(str_word_count($text, 2));
النتيجة:
Array
(
    [0] => Hello
    [6] => world
    [12] => from
    [17] => PHP
)
 استخدام charlist لإضافة أحرف خاصة
مثال يسمح بالعدّ مع كلمات تحتوي أرقامًا:
$text = "PHP7 is better than PHP5";
print_r(str_word_count($text, 1, "0123456789"));
النتيجة:
Array
(
    [0] => PHP7
    [1] => is
    [2] => better
    [3] => than
    [4] => PHP5
)

️ ملاحظة مهمة عن اللغة العربية
دالة str_word_count() لا تدعم العربية بشكل كامل افتراضيًا، لأن PHP تعتبر الكلمات بناءً على أحرف اللغة الإنجليزية.
مثال:
echo str_word_count("مرحبا كيف حالك");
قد ترجع:0
 الحل:
استخدام التعبيرات النمطية (regex) بدلًا منها:
preg_match_all('/\p{Arabic}+/u', "مرحبا كيف حالك", $words);
print_r($words[0]);
🎯 استخدامات شائعة لدالة str_word_count()
✔ عدّ الكلمات في المحتوى النصي
✔ مراقبة طول المقالات
✔ فحص نصوص المدخلات
✔ إنشاء تقارير نصية
✔ استخراج الكلمات لمعالجة لغوية بسيطة

🔵 substr_count() في PH
تستخدم دالة substr_count() في PHP لعدّ عدد مرات ظهور نص معين داخل نص آخر.
وهي مفيدة في تحليل النصوص، والإحصاء، وبناء نظم البحث، والتأكد من وجود كلمات أو رموز مكررة.
تعريف الدالة
substr_count(
    string $haystack,
    string $needle,
    int $offset = 0,
    ?int $length = null
): int

وظيفة الدالة
✔ عدّ عدد مرات ظهور نص (needle) داخل نص كبير (haystack)
✔ يمكن تحديد نقطة بداية البحث (offset)
✔ يمكن تحديد نطاق معين داخل النص (length)
 ملاحظات مهمة
الدالة ليست حساسة للحروف (case-sensitive)
⇒ "PHP" تختلف عن "php"
إذا كان النص فارغًا، ترجع الدالة 0
إذا كانت needle نصًا فارغًا، ترجع تحذيرًا
 أمثلة 
 1. عدّ كلمة داخل نص
$text = "Hello world, Hello PHP";
echo substr_count($text, "Hello");
النتيجة:2
 2. حساب عدد ظهور حرف
echo substr_count("banana", "a");
النتيجة:3
 استخدام offset (بدء البحث من موقع معين)
 3. بدء البحث بعد أول 3 أحرف
echo substr_count("banana", "a", 3);
الناتج:2
لأن البحث يبدأ من الحرف الرابع: "ana"
🔵ثالثاً: استخدام length (تحديد جزء فقط للبحث)
 4. البحث داخل جزء معين فقط
echo substr_count("banana", "a", 1, 3);
هنا:
يبدأ من index = 1 → "anana"
يبحث فقط في 3 أحرف → "ana"
النتيجة:2
مثال عملي شامل
$text = "PHP is great. PHP is powerful. I love PHP!";
echo "عدد مرات ظهور PHP: " . substr_count($text, "PHP") . "<br>";
echo "عدد المرات بعد الحرف 10: " . substr_count($text, "PHP", 10) . "<br>";
echo "البحث في نطاق محدد (من 4 ولغاية 15): " . substr_count($text, "PHP", 4, 15);
النتيجة :
عدد مرات ظهور PHP: 3
عدد المرات بعد الحرف 10: 2
البحث في نطاق محدد (من 4 ولغاية 15): 1
🎯 أهم استخدامات substr_count()
✔ عدّ عدد مرات ظهور كلمة في مقال
✔ تحليل النصوص ومحركات البحث
✔ فلترة النص واكتشاف الكلمات المكررة
✔ بناء نظام إحصاء الكلمات
✔ التحقق من تكرار رموز أو أجزاء معينة داخل النص

🔵 substr() في PHP
تُعد دالة substr() من أهم دوال النصوص في PHP، وتستخدم لاستخراج جزء محدد من سلسلة نصية (String).
تسمح لك الدالة باختيار:
✔ نقطة بداية القص
✔ طول الجزء المراد استخراجه (اختياري)
✔ دعم الأرقام السالبة لاستخراج الجزء من النهاية


تعريف الدالة
substr(string $string, int $start, ?int $length = null): string|false
أمثلة
✔ 1. استخراج أول 5 حروف
echo substr("Hello World", 0, 5);
النتيجة:
Hello
 2. استخراج النص من الموضع 6 حتى النهاية
echo substr("Hello World", 6);
النتيجة:
World

استخدام القيمة السالبة
 3. استخراج آخر 3 أحرف
echo substr("Hello", -3);
النتيجة:llo
 4. أخذ نص من البداية بدون آخر حرفين
echo substr("Hello", 0, -2);
النتيجة:Hel
 ملاحظة مهمة بالنسبة للغة العربية (UTF-8)
دالة substr() قد تقطع الحرف العربي نصفين لأن الحرف العربي يُخزَّن بأكثر من بايت.
 الحل مع النصوص العربية:
استخدم دالة mb_substr():
echo mb_substr("مرحبا بالعالم", 0, 6, "UTF-8");



استخدامات substr()
✔ اقتطاع النصوص الطويلة
✔ عرض جزء من النص في الواجهات
✔ إنشاء أنظمة بحث وفحص للكلمات
✔ تقسيم النصوص وتحليلها
✔ استخراج أرقام أو رموز من سلاسل معينة
🔵 substr_replace() في PHP
تُعد دالة substr_replace() من دوال النصوص المهمة في PHP، وتستخدم لاستبدال جزء محدد من نص معين بنص آخر.
تُعتبر أداة قوية عند الحاجة لتغيير جزء من سلسلة دون استبدالها بالكامل.
تقوم substr_replace() باستبدال جزء من النص حسب:

✔ موضع البداية $start
✔ عدد الأحرف التي سيتم استبدالها $length
✔ بالنص الجديد $replace

  أمثلة 

✔ 1. استبدال جزء من النص
echo substr_replace("Hello World", "PHP", 6);
النتيجة:
Hello PHP
(تم استبدال من index 6 حتى النهاية)
 2. استبدال عدد محدد من الأحرف
echo substr_replace("Hello World", "PHP", 6, 5);
النتيجة:
Hello PHP
(تم استبدال كلمة "World" بالكامل لأنها 5 أحرف)
 استخدام start السالب
 3. بدء الاستبدال من النهاية
echo substr_replace("Hello World", "PHP", -5);
النتيجة:
Hello PHP
(استبدل آخر 5 أحرف)
إدراج نص دون حذف (length = 0)
 4. إدراج نص داخل نص

echo substr_replace("Hello World", "Beautiful ", 6, 0);
Hello Beautiful World
حذف جزء من النص واستبداله بسلسلة فارغة
 5. حذف جزء من النص فقط
echo substr_replace("Hello World", "", 5);
النتيجة:Hello
(تم حذف الجزء بعد index 5)

 6. إخفاء جزء من رقم الهوية
$id = "123456789";
echo substr_replace($id, "****", 3, 4);
النتيجة:
123****89
 7. استبدال نطاق معين باستخدام طول سلبي

echo substr_replace("Programming", "123", 3, -4);
النتيجة:Pro123ming 
(تم استبدال من index 3 واستبعاد آخر 4 حروف من الحذف)
️ ملاحظة حول النصوص العربية (UTF-8)
قد لا تعمل الدالة بشكل صحيح على الأحرف العربية لأنها تعتمد على البايتات وليس الحروف.
 الحل: استخدم الدالة متعددة البايتات:
mb_substr() + concatenation
🎯 أهم استخدامات substr_replace()
✔ تعديل النصوص جزئيًا
✔ إدراج نصوص داخل نص كبير
✔ إخفاء كلمات أو أرقام حساسة
✔ بناء نصوص مخصصة
✔ معالجة النصوص في برامج التحرير
🔵 strpos() – strrpos() – stripos() – strstr()
تُستخدم دوال البحث داخل النصوص في PHP لاكتشاف مواقع الكلمات أو استخراج أجزاء من النص.
الدوال التالية من أشهر وأهم الدوال المستخدمة:
🔹 strpos()
🔹 strrpos()
🔹 stripos()
🔹 strstr()
كل دالة لها وظيفة تختلف قليلًا عن الأخرى، وسنشرحها بالتفصيل.
🟦 أولاً: دالة strpos() — إيجاد أول ظهور للنص
تبحث عن أول موقع يظهر فيه نص (needle) داخل نص كبير (haystack).
✔ حساسة لحالة الأحرف (Case-sensitive)
 مثال 1: موقع كلمة داخل نص
echo strpos("Hello World", "World");
النتيجة:6
 مثال 2: البحث من موضع معين
echo strpos("Hello Hello", "Hello", 2);
النتيجة:6

🟦 ثانياً: دالة strrpos() — إيجاد آخر ظهور للنص
تُرجع آخر موقع يظهر فيه النص.
✔ حساسة لحالة الأحرف
✔ عكس وظيفة strpos()
 مثال
echo strrpos("Hello Hello", "Hello");
النتيجة:6
مثال آخر
echo strrpos("banana", "a");
النتيجة:5

🟦 ثالثاً: دالة stripos() — البحث بدون حساسية للأحرف
تشبه strpos() لكن…
✔ غير حساسة لحالة الحروف
✖ لا تفرق بين “PHP” و “php”
 مثال
echo stripos("Hello World", "world");
النتيجة:6
 مثال: مقارنة بدون حساسية
echo stripos("PHP is great", "php");
النتيجة:0
🟦 رابعاً: دالة strstr() — إرجاع بقية النص من أول ظهور
🔹 ترجع بقية النص بدءًا من أول ظهور للـ needle.
🔹 إذا كان الخيار before_needle = true → ترجع النص قبل الكلمة.



 مثال 1: إرجاع النص بعد الكلمة

echo strstr("Hello World", "World");
النتيجة:
World
 مثال 2: إرجاع النص قبل الكلمة
echo strstr("Hello World", "World", true);
النتيجة:
Hello
 مثال 3: استخدامه للبحث داخل الإيميل
$email = "user@example.com";
echo strstr($email, "@");
النتيجة:
@example.com
🟦 مقارنة  بين الدوال
الدالة	الوظيفة	حساسية الأحرف	ترجع

strpos()	أول موقع	 نعم	رقم الموقع
strrpos()	آخر موقع	 نعم	رقم الموقع
stripos()	أول موقع	   لا	رقم الموقع
strstr()	النص من أول ظهور	 نعم	نص





🎯 أهم استخدامات هذه الدوال
فحص وجود كلمة داخل نص
تحليل واستخراج بيانات من نصوص
التحقق من صحة الإيميل

🔵 أولاً: دالة str_replace()
دالة str_replace() تقوم باستبدال نص بنص آخر داخل سلسلة نصية، حسّاسة لحالة الأحرف (Case Sensitive).
 مثال
<?php
$text = "I love PHP programming";
$result = str_replace("PHP", "Java", $text);
echo $result;
?>
✔ الناتج:
I love Java programming

🔵 ثانيًا: دالة str_ireplace()
نفس عمل str_replace() لكن غير حسّاسة لحالة الأحرف
(Case Insensitive)، أي لا تفرق بين PHP و php و Php.
مثال
<?php
$text = "I Like php and PHP and PhP";
$result = str_ireplace("php", "Java", $text);
echo $result;
?>
الناتج:
I Like Java and Java and Java
🔵 ثالثًا: دالة substr_replace()
تقوم باستبدال جزء من النص بمقدار حدود (بداية – نهاية)،
أي أنها لا تبحث عن كلمة معينة، بل تستبدل حسب الموقع والعدد.
 مثال 1: استبدال جزء من النص
<?php
$text = "Hello World";
$result = substr_replace($text, "PHP", 6, 5);
echo $result;
?>
الشرح
ابدأ من الحرف رقم 6 استبدل 5 أحرف ("World") بالنص "PHP"
 الناتج: Hello PHP
 مثال 2: إضافة نص بدون حذف
<?php
$text = "Hello World";
$result = substr_replace($text, " Beautiful", 5, 0);
echo $result;
?>
 الشرح
موقع 5 = بعد كلمة Hello الطول 0 = لا يتم حذف أي حرف
 الناتج: Hello Beautiful World
 





 دالة strtolower()
تقوم بتحويل كامل النص إلى أحرف صغيرة (lowercase).
 مثال
<?php
$text = "HELLO WORLD";
$result = strtolower($text);
echo $result;
?>
✔ الناتج: hello world
🔵 :دالة strtoupper()
تقوم بتحويل كامل النص إلى أحرف كبيرة (UPPERCASE).
 مثال
<?php
$text = "hello world";
$result = strtoupper($text);
echo $result;
?>
الناتج: HELLO WORLD
🔵 دالة ucfirst()
تقوم بتحويل أول حرف من النص فقط إلى حرف كبير (Capital Letter)،
وتترك باقي النص كما هو.
 مثال
<?php
$text = "hello world";
$result = ucfirst($text);
echo $result;
?>

✔ الناتج: Hello world

🔵  دالة ucwords()
تحول أول حرف من كل كلمة في النص إلى حرف كبير
(مثل عناوين الكتب Titles Case).
 مثال
<?php
$text = "welcome to php programming";
$result = ucwords($text);
echo $result;
?>
✔ الناتج: Welcome To Php Programming
🟢 الفرق بين الدوال الأربعة باختصار
الدالة	الوظيفة	مثال للتحويل
strtolower()	تحويل كل الأحرف إلى صغيرة	HELLO → hello
strtoupper()	تحويل كل الأحرف إلى كبيرة	hello → HELLO
ucfirst()	أول حرف فقط إلى كبير	hello world → Hello world
ucwords()	أول حرف من كل كلمة إلى كبير	hello world → Hello World








🔵 1) دالة trim()
تقوم بإزالة المسافات الفارغة (Spaces) من بداية و نهاية النص،
كما يمكنها إزالة أحرف محددة عند الحاجة.
 مثال
<?php
$text = "   hello world   ";
$result = trim($text);
echo $result;
?>
 الناتج: hello world

🔵 2) دالة ltrim()
تقوم بإزالة المسافات أو الأحرف من بداية النص فقط (Left trim).
 مثال
<?php
$text = "   hello world";
$result = ltrim($text);
echo $result;
?>
 الناتج: hello world

🔵 3) دالة rtrim()
تزيل المسافات أو الأحرف من نهاية النص فقط (Right trim).
 مثال
<?php
$text = "hello world   ";
$result = rtrim($text);
echo $result;
?>
 الناتج: hello world

🔵 4) دالة htmlspecialchars()
تحول الرموز الخاصة في HTML إلى رموز آمنة،
حتى تمنع تشغيلها على المتصفح وتحمي الموقع من XSS.
مثال:
< تتحول إلى &lt;
> تتحول إلى &gt;
" تتحول إلى &quot;
& تتحول إلى &amp;
 الصيغة
htmlspecialchars($string, ENT_QUOTES);
مثال
<?php
$text = "<h1>Hello</h1>";
$result = htmlspecialchars($text);
echo $result;
?>
 الناتج: &lt;h1&gt;Hello&lt;/h1&gt;
 الاستخدام الشائع
عرض نص كتبه المستخدم بدون تشغيله كـ HTML، للحماية من الهجمات.

🔵 5) دالة strip_tags()
تقوم بحذف جميع وسوم HTML / XML من النص
وتُبقي فقط المحتوى النصي.
مثال
<?php
$text = "<h1>Hello</h1> <b>World</b>";
$result = strip_tags($text);
echo $result;
?>
الناتج: Hello World
مثال آخر (السماح بوسم معين)
<?php
$text = "<h1>Hello</h1> <b>World</b>";
$result = strip_tags($text, "<b>");
echo $result;
?>
 الناتج: Hello <b>World</b>
🟢 الفرق بين الدوال باختصار

الدالة	الوظيفة

trim()	إزالة المسافات من البداية والنهاية
ltrim()	إزالة المسافات من البداية فقط
rtrim()	إزالة المسافات من النهاية فقط
htmlspecialchars()	تحويل أكواد HTML إلى نص آمن

🔵 1) دالة explode()
دالة explode() تُستخدم لتحويل سلسلة نصية إلى مصفوفة،
بحيث يتم تقسيم النص حسب فاصل محدد.

 الصيغة
explode(string $delimiter, string $string, int $limit = PHP_INT_MAX): array
$delimiter: الحرف أو النص الذي يستخدم للفصل
$string: النص الأصلي
$limit: اختياري، عدد العناصر القصوى في المصفوفة
 مثال 1: تقسيم نص إلى كلمات
<?php
$text = "apple,banana,orange";
$array = explode(",", $text);
print_r($array);
?>
 الناتج:
Array
(
    [0] => apple
    [1] => banana
    [2] => orange
)

 مثال 2: استخدام limit
$text = "one,two,three,four";
$array = explode(",", $text, 3);
print_r($array);
 الناتج:
Array
(
    [0] => one
    [1] => two
    [2] => three,four
)
> لاحظ أن العنصر الأخير يحتوي على بقية النص.
🔵 2) دالة implode()
تقوم دالة implode() بتحويل مصفوفة إلى نص،
بحيث يتم دمج العناصر باستخدام فاصل معين.
implode(string $glue, array $pieces): string
$glue: النص الذي سيتم استخدامه للفصل بين العناصر
$pieces: المصفوفة المراد تحويلها إلى نص
 مثال 1: دمج مصفوفة إلى نص
<?php
$array = ["apple", "banana", "orange"];
$text = implode(",", $array);
echo $text;
?>
 الناتج: apple,banana,orange
 مثال 2: استخدام فاصل مختلف
$array = ["PHP", "Java", "Python"];
$text = implode(" - ", $array);
echo $text;
 الناتج: PHP - Java - Python
⚡ استخدامات شائعة
تحليل CSV أو البيانات المفصولة بفواصل
تحويل نصوص المستخدم إلى مصفوفة
دمج بيانات المصفوفة لعرضها أو حفظها
التعامل مع كلمات أو عناوين أو قوائم
🔵 1) دالة strcmp()
تقوم strcmp() بمقارنة سلسلتين نصيتين حساساً لحالة الأحرف (Case Sensitive) وترجع:
0 إذا كانت السلسلتان متطابقتين
عدد سالب إذا كانت السلسلة الأولى أقل من الثانية
عدد موجب إذا كانت السلسلة الأولى أكبر من الثانية
 مثال 1: المقارنة الحساسة لحالة الأحرف
<?php
$str1 = "Hello";
$str2 = "hello";
$result = strcmp($str1, $str2);
echo $result;
?>

 الناتج: -1
> لأن "H" أكبر من "h" في ASCII.
 مثال 2: نصوص متطابقة
echo strcmp("PHP", "PHP");

 الناتج: 0

🔵 2) دالة strcasecmp()
تشبه strcmp() لكنها غير حساسة لحالة الأحرف (Case Insensitive).
> أي تتجاهل الفرق بين "PHP" و "php".
 مثال
<?php
$str1 = "Hello";
$str2 = "hello";

$result = strcasecmp($str1, $str2);
echo $result;
?>
 الناتج: 0
> السلسلتان تعتبران متطابقتين بدون حساسية للحروف.

🔵 3) دالة similar_text()
تحسب نسبة التشابه بين سلسلتين نصيتين، ويمكن إرجاع عدد الحروف المتطابقة وعدد النسبة المئوية للتشابه.
$percent: متغير لإرجاع نسبة التشابه المئوية.
 مثال 1: قياس التشابه
<?php
$str1 = "Hello World";
$str2 = "Hello PHP";
similar_text($str1, $str2, $percent);
echo "عدد الحروف المتطابقة: " . similar_text($str1, $str2);
echo "\nنسبة التشابه: " . $percent . "%";
?>
 الناتج المتوقع: عدد الحروف المتطابقة: 6
نسبة التشابه: 54.545454545455%
 مثال 2: نصوص متطابقة تقريبًا
similar_text("PHP Programming", "PHP Programing", $percent);
echo $percent;
 الناتج: 92.307692307692%



⚡ استخدامات شائعة
التحقق من تطابق كلمات المرور
مقارنة نصوص المستخدم أو المدخلات
قياس درجة التشابه بين النصوص أو العناوين
البحث عن تشابه في محتوى النصوص أو الملفات

🔵 1) دالة md5()
دالة md5() تقوم بإنشاء تجزئة (Hash) بطول 32 حرفًا (128-bit) من النص، وتُستخدم عادة لتشفير كلمات المرور أو حفظ البيانات بشكل آمن (غير قابل للقراءة).
>  ملاحظة: MD5 قديمة نوعًا ما وغير آمنة للتطبيقات الحساسة؛ يفضل استخدام password_hash() أو خوارزميات أحدث.
md5(string $str, bool $raw_output = false): string
$str: النص المراد تجزئته
$raw_output: إذا كانت true، تُرجع البايتات الأصلية بدل سلسلة hex
 مثال 1: تجزئة نص عادي
<?php
$text = "HelloWorld";
$hash = md5($text);
echo $hash;
?>
 الناتج (مثال)
68e109f0f40ca72a15e05cc22786f8e6
مثال 2: التجزئة في البايتات الخام
$hash = md5($text, true);
echo $hash;
✔ الناتج أحرف غير قابلة للقراءة (Binary)


🔵 2) دالة sha1()
دالة sha1() تقوم بإنشاء تجزئة SHA-1 طولها 40 حرفًا (160-bit).
SHA-1 أقوى من MD5 قليلًا ولكنها أيضًا ليست مضمونة للتطبيقات الحساسة.
sha1(string $str, bool $raw_output = false): string
مثال 1: تجزئة نص
<?php
$text = "HelloWorld";
$hash = sha1($text);
echo $hash;
?>
 الناتج (مثال)
0a4d55a8d778e5022fab701977c5d840bbc486d0
 مثال 2: التجزئة بالبايتات الخام
$hash = sha1($text, true);
echo $hash;
 الناتج
أحرف غير قابلة للقراءة (Binary)
> ملاحظة هامة: لا تستخدم MD5 أو SHA-1 لحفظ كلمات المرور مباشرة. استخدم دائمًا password_hash() و password_verify() لتأمين كلمات المرور.

حفظ كلمات المرور (مع password_hash أفضل)
التحقق من سلامة الملفات (Hash Verification)
إنشاء معرفات فريدة (Unique ID)
حماية البيانات غير الحساسة من التغيير




دوال المصفوفات 
إليك بحث شامل عن دوال المصفوفات في PHP: count() – is_array() – array_values() – array_keys() مع الشرح والأمثلة العملية.

🔵 1) دالة count()
تقوم دالة count() بإرجاع عدد العناصر في المصفوفة.
count(array $array, int $mode = COUNT_NORMAL): int
$array: المصفوفة المراد حساب عناصرها
$mode: اختياري (COUNT_RECURSIVE لحساب العناصر في المصفوفة متعددة الأبعاد)
 مثال 1: حساب عدد عناصر مصفوفة بسيطة
<?php
$arr = ["apple", "banana", "orange"];
echo count($arr);
?>
 الناتج: 3
 مثال 2: مصفوفة متعددة الأبعاد
$arr = ["fruits" => ["apple", "banana"], "vegetables" => ["carrot"]];
echo count($arr, COUNT_RECURSIVE);
 الناتج: 4

🔵 2) دالة is_array()
تتحقق دالة is_array() مما إذا كانت القيمة مصفوفة أم لا، وترجع true أو false.
is_array(mixed $var): bool





 مثال
<?php
$arr = ["apple", "banana"];
$text = "Hello";
echo is_array($arr) ? "Yes" : "No";
echo "\n";
echo is_array($text) ? "Yes" : "No";
?>

 الناتج:
Yes
No

🔵 3) دالة array_values()
تقوم array_values() بإرجاع قيم المصفوفة فقط بدون المفاتيح.
array_values(array $array): array
 مثال
<?php
$arr = ["a" => "apple", "b" => "banana", "c" => "orange"];
$values = array_values($arr);
print_r($values);
?>
 الناتج:
Array
(
    [0] => apple
    [1] => banana
    [2] => orange
)


🔵 4) دالة array_keys()
تقوم array_keys() بإرجاع مفاتيح المصفوفة في مصفوفة جديدة، ويمكن البحث عن مفتاح معين أيضًا.
array_keys(array $array, mixed $search_value = null, bool $strict = false): array
$search_value: اختياري، لإرجاع المفاتيح التي تحتوي هذه القيمة فقط
$strict: اختياري، لمراعاة النوع (Type)
 مثال 1: جميع المفاتيح
<?php
$arr = ["a" => "apple", "b" => "banana", "c" => "orange"];
$keys = array_keys($arr);
print_r($keys);
?>
 الناتج:
Array
(
    [0] => a
    [1] => b
    [2] => c
)
 مثال 2: البحث عن قيمة محددة
$arr = ["a" => "apple", "b" => "banana", "c" => "apple"];
$keys = array_keys($arr, "apple");
print_r($keys);

 الناتج:
Array
(
    [0] => a
    [1] => c
)

⚡ استخدامات شائعة
تحليل البيانات والمصفوفات
التحقق من صحة المدخلات
العمل مع الجداول (Array) في PHP
البحث عن القيم والمفاتيح
التعامل مع المصفوفات متعددة الأبعاد

🔵 1) دالة array_push()
تقوم array_push() بإضافة عنصر أو أكثر إلى نهاية المصفوفة.
array_push(array &$array, mixed ...$values): int
$array: المصفوفة المراد تعديلها
$values: العناصر المراد إضافتها
تُرجع عدد العناصر بعد الإضافة.
مثال
<?php
$arr = ["apple", "banana"];
array_push($arr, "orange", "grape");
print_r($arr);
?>

 الناتج:
Array
(
    [0] => apple
    [1] => banana
    [2] => orange
    [3] => grape
)

🔵 2) دالة array_pop()
 التعريف
تقوم array_pop() بحذف آخر عنصر من المصفوفة وترجع قيمته.
array_pop(array &$array): mixed
 مثال
<?php
$arr = ["apple", "banana", "orange"];
$last = array_pop($arr);
echo "Last element: $last\n";
print_r($arr);
?>
 الناتج:
Last element: orange
Array
(
    [0] => apple
    [1] => banana
)
🔵 3) دالة array_shift()
تقوم array_shift() بحذف أول عنصر من المصفوفة وترجع قيمته، ويُعاد ترتيب المفاتيح تلقائيًا.
array_shift(array &$array): mixed
 مثال
<?php
$arr = ["apple", "banana", "orange"];
$first = array_shift($arr);
echo "First element: $first\n";
print_r($arr);
?>
 الناتج:
First element: apple
Array
(
    [0] => banana
    [1] => orange
)

🔵 4) دالة array_unshift()
تقوم array_unshift() بإضافة عنصر أو أكثر في بداية المصفوفة.
array_unshift(array &$array, mixed ...$values): int
تُرجع عدد العناصر بعد الإضافة
 مثال
<?php
$arr = ["banana", "orange"];
array_unshift($arr, "apple", "grape");
print_r($arr);
?>
 الناتج:
Array
(
    [0] => apple
    [1] => grape
    [2] => banana
    [3] => orange
)

🔵 5) دالة unset()
تقوم unset() بحذف عنصر محدد من المصفوفة أو متغير بالكامل.
unset($array[$key]);
 مثال 1: حذف عنصر من المصفوفة
<?php
$arr = ["apple", "banana", "orange"];
unset($arr[1]);
print_r($arr);
?>
 الناتج:
Array
(
    [0] => apple
    [2] => orange
)
 لاحظ أن المفاتيح لا تُعاد ترتيبها تلقائيًا بعد unset.

 مثال 2: حذف متغير كامل
<?php
$name = "John";
unset($name);
echo $name; // سيظهر خطأ لأن المتغير تم حذفه
?>
⚡ استخدامات شائعة
إدارة عناصر المصفوفة (Stack – Queue)
إضافة وحذف البيانات في القوائم
تعديل المصفوفات ديناميكيًا
إزالة العناصر الغير مرغوبة أو المؤقتة

🔵 1) دالة in_array()
تتحقق دالة in_array() مما إذا كانت قيمة معينة موجودة داخل المصفوفة، وترجع true أو false.
in_array(mixed $needle, array $haystack, bool $strict = false): bool
$needle: القيمة المراد البحث عنها
$haystack: المصفوفة التي سيتم البحث فيها
$strict: إذا كانت true، تتحقق من النوع أيضًا
 مثال
<?php
$arr = ["apple", "banana", "orange"];
if (in_array("banana", $arr)) {
    echo "Found!";
} else {
    echo "Not found!";
}
?>

الناتج: Found!

🔵 2) دالة array_search()
تبحث دالة array_search() عن قيمة في المصفوفة وتُرجع المفتاح الخاص بها إذا وجدت، وإلا تُرجع false.
array_search(mixed $needle, array $haystack, bool $strict = false): int|string|false
$needle: القيمة المراد البحث عنها
$haystack: المصفوفة
$strict: التحقق من النوع أيضًا
 مثال 1: البحث عن قيمة
<?php
$arr = ["a" => "apple", "b" => "banana", "c" => "orange"];
$key = array_search("banana", $arr);
echo $key;
?>
 الناتج: b
 مثال 2: البحث مع النوع الصارم
$arr = [1, 2, "3"];
$key = array_search(3, $arr, true); // نوع البيانات مهم
var_dump($key);
 الناتج: bool(false)
> لأن "3" (String) != 3 (Integer) عند استخدام $strict = true.



---

🔵 3) دالة array_key_exists()
تتحقق دالة array_key_exists() مما إذا كان مفتاح معين موجود في المصفوفة، وترجع true أو false.
array_key_exists(mixed $key, array $array): bool
$key: المفتاح المراد التحقق منه
$array: المصفوفة
✔ مثال
<?php
$arr = ["a" => "apple", "b" => "banana"];
if (array_key_exists("b", $arr)) {
    echo "Key exists!";
} else {
    echo "Key does not exist!";
}
?>
 الناتج: Key exists!
⚡ استخدامات شائعة
التحقق من وجود عنصر قبل استخدامه
البحث عن مفاتيح أو قيم محددة
معالجة المصفوفات الديناميكية
البحث عن البيانات في الجداول والمصفوفات

🔵 1) دالة sort()
تقوم sort() بترتيب المصفوفة تصاعديًا حسب القيم وتُعيد ترتيب المفاتيح تلقائيًا.
sort(array &$array, int $sort_flags = SORT_REGULAR): bool
 مثال
<?php
$arr = [3, 1, 4, 2];
sort($arr);
print_r($arr);
?>
 الناتج:
Array
(
    [0] => 1
    [1] => 2
    [2] => 3
    [3] => 4
)
🔵 2) دالة rsort()
تقوم rsort() بترتيب المصفوفة تنازليًا حسب القيم.
 مثال
$arr = [3, 1, 4, 2];
rsort($arr);
print_r($arr);
 الناتج:
Array
(
    [0] => 4
    [1] => 3
    [2] => 2
    [3] => 1
)


🔵 3) دالة asort()
ترتب القيم تصاعديًا مع الحفاظ على المفاتيح الأصلية.
مثال
$arr = ["a"=>3, "b"=>1, "c"=>4];
asort($arr);
print_r($arr);
 الناتج:
Array
(
    [b] => 1
    [a] => 3
    [c] => 4
)
🔵 4) دالة ksort()
ترتب المصفوفة حسب المفاتيح تصاعديًا.
مثال
$arr = ["c"=>3, "a"=>1, "b"=>2];
ksort($arr);
print_r($arr);
الناتج:
Array
(
    [a] => 1
    [b] => 2
    [c] => 3
)

🔵 5) دالة arsort()
ترتب القيم تنازليًا مع الحفاظ على المفاتيح الأصلية.
 مثال
$arr = ["a"=>3, "b"=>1, "c"=>4];
arsort($arr);
print_r($arr);
 الناتج:
Array
(
    [c] => 4
    [a] => 3
    [b] => 1
)

🔵 6) دالة krsort()
ترتب المصفوفة حسب المفاتيح تنازليًا.
✔ مثال
$arr = ["c"=>3, "a"=>1, "b"=>2];
krsort($arr);
print_r($arr);
 الناتج:
Array
(
    [c] => 3
    [b] => 2
    [a] => 1
)
⚡ استخدامات شائعة
ترتيب القوائم الرقمية أو النصية
ترتيب الجداول حسب القيم أو المفاتيح
إنشاء واجهات مستخدم مرتبة
معالجة البيانات في قواعد البيانات والمصفوفات

🔵 1) دالة array_merge()
تقوم array_merge() بدمج مصفوفتين أو أكثر في مصفوفة واحدة.
array_merge(array ...$arrays): array
 مثال
<?php
$arr1 = ["apple", "banana"];
$arr2 = ["orange", "grape"];
$result = array_merge($arr1, $arr2);
print_r($result);
?>
 الناتج:
Array
(
    [0] => apple
    [1] => banana
    [2] => orange
    [3] => grape
)



🔵 2) دالة array_combine()
تقوم array_combine() بإنشاء مصفوفة جديدة باستخدام مصفوفة للمفاتيح وأخرى للقيم.
array_combine(array $keys, array $values): array
 مثال
<?php
$keys = ["a", "b", "c"];
$values = ["apple", "banana", "orange"];
$result = array_combine($keys, $values);
print_r($result);
?>
 الناتج:
Array
(
    [a] => apple
    [b] => banana
    [c] => orange
)

🔵 3) دالة array_chunk()
تقسم المصفوفة إلى مصفوفات أصغر بحجم محدد.
array_chunk(array $array, int $size, bool $preserve_keys = false): array
$size: حجم كل قطعة
$preserve_keys: للحفاظ على المفاتيح الأصلية




 مثال
<?php
$arr = [1, 2, 3, 4, 5];
$result = array_chunk($arr, 2);
print_r($result);
?>
 الناتج:
Array
(
    [0] => Array
        (
            [0] => 1
            [1] => 2
        )
    [1] => Array
        (
            [0] => 3
            [1] => 4
        )
    [2] => Array
        (
            [0] => 5
        )
)




🔵 4) دالة array_slice()
تقوم array_slice() باستخراج جزء من المصفوفة بدون تعديل الأصلية.
array_slice(array $array, int $offset, ?int $length = null, bool $preserve_keys = false): array
$offset: موقع البداية
$length: عدد العناصر
$preserve_keys: الحفاظ على المفاتيح الأصلية
 مثال
<?php
$arr = [1, 2, 3, 4, 5];
$result = array_slice($arr, 2, 2);
print_r($result);
?>
 الناتج:
Array
(
    [0] => 3
    [1] => 4
)

🔵 5) دالة array_splice()
تقوم array_splice() بحذف جزء من المصفوفة واستبداله بأخرى، وتغير المصفوفة الأصلية.
array_splice(array &$array, int $offset, ?int $length = null, array $replacement = []): array



 مثال 1: حذف جزء من المصفوفة
<?php
$arr = [1, 2, 3, 4, 5];
$removed = array_splice($arr, 1, 2); // حذف عنصران ابتداءً من index=1
print_r($arr);
print_r($removed);
?>
 الناتج:
$arr:
Array
(
    [0] => 1
    [1] => 4
    [2] => 5
)
$removed:
Array
(
    [0] => 2
    [1] => 3
)

مثال 2: استبدال جزء من المصفوفة
<?php
$arr = [1, 2, 3, 4, 5];
array_splice($arr, 2, 2, ["a","b"]); 
print_r($arr);
?>
 الناتج:
Array
(
    [0] => 1
    [1] => 2
    [2] => a
    [3] => b
    [4] => 5
)

الفرق بين الدوال باختصار
الدالة	الوظيفة	تعديل المصفوفة الأصلية؟
array_merge()	دمج مصفوفتين أو أكثر	نعم (تُرجع مصفوفة جديدة)
array_combine()	إنشاء مصفوفة من مفاتيح وقيم	نعم (تُرجع مصفوفة جديدة)
array_chunk()	تقسيم المصفوفة إلى مصفوفات أصغر	لا، تُرجع مصفوفة جديدة
array_slice()	استخراج جزء من المصفوفة	لا، تُرجع مصفوفة جديدة
array_splice()	حذف/استبدال جزء من المصفوفة	نعم، تعدل الأصلية

⚡ استخدامات شائعة
تقسيم البيانات إلى مجموعات صغيرة
دمج البيانات أو إنشاء مصفوفات من مفاتيح وقيم
تعديل المصفوفات ديناميكيًا
استخراج جزء معين للعرض أو المعالجة




🔵 1) دالة array_filter()Key exists!
تقوم array_filter() بتصفية المصفوفة وإرجاع عناصر تحقق شرطًا معينًا باستخدام دالة callback.
إذا لم تحدد دالة callback، تقوم بحذف القيم "الزائفة" (مثل 0, false, null, "").
array_filter(array $array, ?callable $callback = null, int $mode = 0): array
 مثال 1: حذف القيم الفارغة
<?php
$arr = [0, 1, 2, "", 3, null, false];
$result = array_filter($arr);
print_r($result);
?>
الناتج:
Array
(
    [1] => 1
    [2] => 2
    [4] => 3
)
 مثال 2: استخدام دالة callback
<?php
$arr = [1, 2, 3, 4, 5];
$result = array_filter($arr, function($num) {
    return $num % 2 == 0; // الاحتفاظ بالأعداد الزوجية فقط
});
print_r($result);
?>


 الناتج:
Array
(
    [1] => 2
    [3] => 4
)

🔵 2) دالة array_map()
تقوم array_map() بتطبيق دالة معينة على كل عنصر في المصفوفة وإرجاع مصفوفة جديدة بالنتائج.
array_map(callable $callback, array $array, array ...$arrays): array
 مثال 1: تطبيق دالة على كل عنصر
<?php
$arr = [1, 2, 3, 4];
$result = array_map(function($num) {
    return $num * 2;
}, $arr);
print_r($result);
?>
 الناتج:
Array
(
    [0] => 2
    [1] => 4
    [2] => 6
    [3] => 8
)

 مثال 2: استخدام أكثر من مصفوفة
$arr1 = [1, 2, 3];
$arr2 = [4, 5, 6];
$result = array_map(function($a, $b) {
    return $a + $b;
}, $arr1, $arr2);
print_r($result);

الناتج:
Array
(
    [0] => 5
    [1] => 7
    [2] => 9
)

🔵 3) دالة array_reduce()
تقوم array_reduce() بتجميع عناصر المصفوفة إلى قيمة واحدة باستخدام دالة callback.
array_reduce(array $array, callable $callback, mixed $initial = null): mixed
$initial: القيمة الابتدائية للتجميع (اختياري)

 مثال 1: جمع عناصر المصفوفة
<?php
$arr = [1, 2, 3, 4];
$sum = array_reduce($arr, function($carry, $item) {
    return $carry + $item;
}, 0);
echo $sum;
?>
 الناتج: 10
 مثال 2: دمج النصوص
$arr = ["Hello", "World", "PHP"];
$result = array_reduce($arr, function($carry, $item) {
    return $carry . " " . $item;
});
echo $result;

 الناتج: Hello World PHP

🟢 الفرق بين الدوال الثلاث باختصار
      الدالة	                            الوظيفة	                                                                      الإرجاع
                array_filter()تصفية المصفوفة بناءً على شرط	مصفوفة جديدة بالعناصر المفلترة
                array_map()تطبيق دالة على كل عنصر.     	مصفوفة جديدة بالنتائج
            array_reduce()دمج عناصر المصفوفة                       قيمة واحدة (Number/String/… إلخ)
                                      إلى قيمة واحدة
⚡ استخدامات شائعة
تصفية البيانات حسب شروط محددة
تعديل أو تحويل كل عنصر في المصفوفة
حساب المجموع أو التجميع أو دمج النصوص
معالجة البيانات بشكل ديناميكي وفعال




🔵 1) دالة array_unique()
تقوم array_unique() بحذف القيم المكررة من المصفوفة وإرجاع مصفوفة جديدة تحتوي على القيم الفريدة فقط.
array_unique(array $array, int $sort_flags = SORT_STRING): array
 مثال
<?php
$arr = [1, 2, 2, 3, 4, 4, 5];
$result = array_unique($arr);
print_r($result);
?>
 الناتج:
Array
(
    [0] => 1
    [1] => 2
    [3] => 3
    [4] => 4
    [6] => 5
)

> لاحظ أن المفاتيح الأصلية تبقى كما هي.

🔵 2) دالة array_intersect()
تقوم array_intersect() بإرجاع المصفوفة التي تحتوي على القيم المشتركة بين مصفوفتين أو أكثر.
array_intersect(array $array1, array ...$arrays): array



 مثال
<?php
$arr1 = [1, 2, 3, 4];
$arr2 = [3, 4, 5, 6];
$result = array_intersect($arr1, $arr2);
print_r($result);
?>
 الناتج:
Array
(
    [2] => 3
    [3] => 4
)

🔵 3) دالة array_diff()
تقوم array_diff() بإرجاع العناصر الموجودة في المصفوفة الأولى وغير الموجودة في بقية المصفوفات.
array_diff(array $array1, array ...$arrays): array
مثال
<?php
$arr1 = [1, 2, 3, 4];
$arr2 = [3, 4, 5, 6];
$result = array_diff($arr1, $arr2);
print_r($result);
?>



 الناتج:
Array
(
    [0] => 1
    [1] => 2
)
🟢 الفرق والوظائف الأساسية باختصار
الدالة	الوظيفة	مخرجاتها
            array_unique()	إزالة القيم المكررة             	                          مصفوفة بالقيم الفريدة
          array_intersect()	القيم المشتركة بين مصفوفتين أو أكثر	مصفوفة بالقيم المشتركة
array_diff()                    القيم الموجودة في مصفوفة وغير                  مصفوفة بالقيم الفريدة للأولى
                                         موجودة في الأخرى	

⚡ استخدامات شائعة
إزالة القيم المكررة من البيانات
معرفة القيم المشتركة بين مجموعتين
مقارنة القوائم واستخراج الاختلافات
معالجة البيانات قبل الحفظ أو العرض

🔵 1) دالة current()
تعيد دالة current() قيمة العنصر الحالي في المصفوفة دون تغيير المؤشر.
current(array $array): mixed
 مثال
<?php
$arr = [1, 2, 3, 4];
echo current($arr);
?>
 الناتج: 1

🔵 2) دالة next()
تقوم next() بتحريك المؤشر إلى العنصر التالي في المصفوفة وتعيد قيمته.
next(array $array): mixed
 مثال
<?php
$arr = [1, 2, 3, 4];
echo next($arr); // ينتقل للعنصر الثاني
?>
 الناتج: 2

🔵 3) دالة prev()
تقوم prev() بتحريك المؤشر إلى العنصر السابق وإرجاع قيمته.
prev(array $array): mixed
 مثال
<?php
$arr = [1, 2, 3, 4];
next($arr); // المؤشر عند 2
next($arr); // المؤشر عند 3
echo prev($arr); // يرجع إلى 2
?>
 الناتج: 2




🔵 4) دالة end()
تقوم end() بتحريك المؤشر إلى آخر عنصر في المصفوفة وإرجاع قيمته.
end(array $array): mixed
 مثال
<?php
$arr = [1, 2, 3, 4];
echo end($arr);
?>

 الناتج: 4

🔵 5) دالة reset()
تقوم reset() بتحريك المؤشر إلى أول عنصر في المصفوفة وإرجاع قيمته.
reset(array $array): mixed
 مثال
<?php
$arr = [1, 2, 3, 4];
end($arr); // المؤشر عند العنصر الأخير
echo reset($arr); // يرجع إلى العنصر الأول
?>
الناتج: 1

⚡ استخدامات شائعة
التنقل بين عناصر المصفوفة يدويًا
استخدام المصفوفة كمؤشر أو قائمة انتظار
معالجة البيانات خطوة خطوة
التعامل مع المصفوفات غير المرتبة أو المعقدة


