
🔹 مقدمة

تُستخدم دوال الوقت والتاريخ في PHP للتعامل مع:
•	عرض التاريخ والوقت
•	حساب الفروق الزمنية
•	تنسيق التاريخ
•	التعامل مع المناطق الزمنية (Time Zones)


🔹 أهم دوال الوقت والتاريخ في PHP

1️⃣ الدالة date()
تُستخدم لعرض التاريخ أو الوقت الحالي بصيغة محددة.
الصيغة:
date(format, timestamp)

مثال:
echo date("Y-m-d");
📌 الناتج:
2025-12-15

أشهر رموز التنسيق:
الرمز	المعنى
Y	السنة (4 أرقام)
y	السنة (رقمين)
m	الشهر
d	اليوم
H	الساعة (24)
h	الساعة (12)
i	الدقائق
s	الثواني


مثال:
echo date("Y-m-d H:i:s");


2️⃣ الدالة time()
ترجع عدد الثواني منذ 1/1/1970 (Unix Timestamp).

echo time();
📌 مثال استخدام:

$now = time();
echo date("Y-m-d H:i:s", $now);


3️⃣ الدالة strtotime()
تحوّل نص تاريخ إلى timestamp.

مثال:
echo strtotime("2025-12-15");



أمثلة مهمة:
echo date("Y-m-d", strtotime("+1 day"));
echo date("Y-m-d", strtotime("+1 week"));
echo date("Y-m-d", strtotime("-2 months"));


4️⃣ الدالة mktime()
تُنشئ timestamp من وقت وتاريخ محدد.

الصيغة:

mktime(hour, minute, second, month, day, year)

مثال:

$time = mktime(10, 30, 0, 12, 15, 2025);
echo date("Y-m-d H:i:s", $time);


---

5️⃣ الدالة getdate()

ترجع مصفوفة تحتوي على تفاصيل التاريخ.

print_r(getdate());

📌 الناتج يحتوي:

year

month

mday

weekday

hours

minutes



---

6️⃣ الدالة checkdate()

تتحقق من صحة التاريخ.

الصيغة:

checkdate(month, day, year)

مثال:

if (checkdate(2, 29, 2024)) {
    echo "تاريخ صحيح";
} else {
    echo "تاريخ غير صحيح";
}


---

7️⃣ التعامل مع المنطقة الزمنية (Timezone)

عرض المنطقة الحالية:

echo date_default_timezone_get();

تغيير المنطقة الزمنية:

date_default_timezone_set("Asia/Riyadh");
echo date("Y-m-d H:i:s");

📌 مناطق شائعة:

Asia/Riyadh

Africa/Cairo

Asia/Aden



---

8️⃣ كلاس DateTime (طريقة احترافية)

طريقة حديثة وقوية للتعامل مع التاريخ.

$date = new DateTime();
echo $date->format("Y-m-d H:i:s");

إضافة أيام:

$date->modify("+3 days");
echo $date->format("Y-m-d");

الفرق بين تاريخين:

$d1 = new DateTime("2025-12-01");
$d2 = new DateTime("2025-12-15");

$diff = $d1->diff($d2);
echo $diff->days . " days";




🔹 مثال تطبيقي بسيط

date_default_timezone_set("Asia/Aden");

echo "اليوم: " . date("l") . "<br>";
echo "التاريخ: " . date("Y-m-d") . "<br>";
echo "الوقت: " . date("H:i:s");




🔹 خلاصة

✔ PHP توفر دوال قوية للتعامل مع الوقت والتاريخ
✔ date() و time() الأكثر استخدامًا
✔ DateTime أفضل للمشاريع الكبيرة
✔ مهم ضبط المنطقة الزمنية دائمًا

