
📌 بحث عن الاتصال بقاعدة البيانات MySQL في PHP



🔹 مقدمة

تعتبر قواعد البيانات جزءًا أساسيًا من أي تطبيق ويب، حيث تُستخدم لتخزين واسترجاع البيانات بشكل منظم.
PHP توفر دوال وأدوات قوية للاتصال بقاعدة البيانات MySQL، سواء لإضافة البيانات، تعديلها، حذفها أو عرضها.
الاتصال السليم بقاعدة البيانات هو أول خطوة لتطوير أي موقع ديناميكي.



🔹 طرق الاتصال بقاعدة البيانات في PHP

PHP توفر طريقتين أساسيتين للتعامل مع MySQL:
1. mysqli (MySQL Improved)
2. PDO (PHP Data Objects)



1️⃣ الاتصال باستخدام mysqli
أ‌- الاتصال بطريقة Procedural (إجرائية)

<?php
$servername = "localhost";
$username = "root";
$password = "";
$dbname = "mydatabase";

$conn = mysqli_connect($servername, $username, $password, $dbname);

if (!$conn) {
    die("فشل الاتصال: " . mysqli_connect_error());
}

echo "تم الاتصال بنجاح!";
?>

ب‌- الاتصال بطريقة Object-Oriented (كائنية)

<?php
$conn = new mysqli("localhost", "root", "", "mydatabase");

if ($conn->connect_error) {
    die("فشل الاتصال: " . $conn->connect_error);
}

echo "تم الاتصال بنجاح!";
?>

تشغيل استعلام:

$sql = "SELECT * FROM users";
$result = $conn->query($sql);

if ($result->num_rows > 0) {
    while($row = $result->fetch_assoc()) {
        echo "الاسم: " . $row["name"] . "<br>";
    }
} else {
    echo "لا توجد نتائج";
}

$conn->close();



2️⃣ الاتصال باستخدام PDO
إنشاء الاتصال:

<?php
try {
    $conn = new PDO("mysql:host=localhost;dbname=mydatabase", "root", "");
    $conn->setAttribute(PDO::ATTR_ERRMODE, PDO::ERRMODE_EXCEPTION);
    echo "تم الاتصال بنجاح!";
} catch(PDOException $e) {
    echo "فشل الاتصال: " . $e->getMessage();
}
?>

تشغيل استعلام:

$stmt = $conn->prepare("SELECT * FROM users");
$stmt->execute();

$result = $stmt->fetchAll(PDO::FETCH_ASSOC);

foreach ($result as $row) {
    echo "الاسم: " . $row['name'] . "<br>";
}



🔹 نصائح عند الاتصال بقاعدة البيانات
تحقق دائمًا من نجاح الاتصال قبل تنفيذ أي استعلام.
استخدم Prepared Statements لحماية الموقع من هجمات SQL Injection.
أغلق الاتصال بعد الانتهاء من العمليات لتوفير الموارد.
استخدم PDO عند تطوير مشاريع كبيرة أو متعددة قواعد البيانات.



🔹 خاتمة
الاتصال بقاعدة بيانات MySQL في PHP يعتبر خطوة أساسية لأي مشروع ويب ديناميكي.
PHP توفر أكثر من طريقة للاتصال مع ميزات متعددة للأمان والمرونة، ويُفضل استخدام PDO للمشاريع الاحترافية.
فهم طرق الاتصال والاستعلامات وإدارة الأخطاء يسهل تطوير تطبيقات آمنة وسريعة وفعالة.

